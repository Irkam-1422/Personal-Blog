<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head.ejs') %>
<body>
    <%- include('./partials/header.ejs') %>


    <h1>CONTACTS</h1>
    <!-- <% const name = 'Jack' %> -->
    <ul>
        <% if (contacts.length) { %>
            <% contacts.forEach(({name, link}) => { %>
                <li><a href="<%= link %>"><%= name %></a></li>
            <% }) %>
        <% } %>
        <!-- <li>phone: +310000000</li>
        <li>email: abcde_@gmail.com</li> -->
        <!-- <li><%= name %></li> -->
    </ul>
    <!-- <style>
         a {
            text-decoration: none;
            color: aliceblue;
            font-family: 'Montserrat', sans-serif;
        }
        body {
            display: flex;
            min-height: 100vh;
            align-items: center;
            flex-direction: column;
            font-family: 'Montserrat', sans-serif;
            justify-content: center;
            font-size: 40px;
            color: aliceblue;
            background: linear-gradient(to bottom, #00d2ff, #928dab);
        }

        h1 {
            text-transform: uppercase;
        }

        header {
            background: #133449;
            opacity: 0.75;
            width: 100%;
            height: 72px;
            display: flex;
            flex-direction: row-reverse;
        }

        button {
            font-size: 20px;
            color: white;
            border: none;
            background: #133449;
            margin: 20px;
            font-weight: 100;
        }
    </style> -->
</body>
</html>